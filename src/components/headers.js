import React from "react";

function Header(props)
{
    return (
        <div>
            {/* <h1>My names are : {props.firstname} {props.surname}</h1>; */}
            {/* <h3>{props.children}</h3> */}
            {/* <button onClick={props.callMe}>Talk To Me</button> */}
        </div>
    )
}

export default Header;